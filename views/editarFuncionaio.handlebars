<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar funcionario</title>
</head>
<body>
    
    <h1>Editar Funcionário</h1>

<form action="/funcionarios/{{funcionario.id}}" method="POST">

    <label>Nome:</label>
    <input type="text" name="nome" value="{{funcionario.nome}}" required>

    <label>Cargo:</label>
    <input type="text" name="cargo" value="{{funcionario.cargo}}" required>

    <label>Salário:</label>
    <input type="number" name="salario" step="0.01" value="{{funcionario.salario}}" required>

    <button type="submit">Salvar Alterações</button>
</form>

</body>
</html>